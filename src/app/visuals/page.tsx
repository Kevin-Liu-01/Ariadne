"use client";

import { VisualsScreen } from "@/app/visuals/visuals-screen";

/** Standalone venue ambient screen. The board's "visuals" scene renders the same component. */
export default function VisualsPage() {
  return <VisualsScreen />;
}
